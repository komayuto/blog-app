= link_to article_path(id: article.id) do
  .card
    - if article.eyecatch.attached?
      -# もしarticleの記事のアイキャッチ画像が設定されていたら
      .card_image
        = image_tag article.eyecatch
        -# アイキャッチ画像の表示
    .card_content
      .card_title
        = article.title
      .card_heart
        = image_tag 'heart.svg'
        %span= article.like_count
        -# いいねの数のカウント
      .card_detail
        = image_tag article.user.avatar_image
        %div
          %p= article.author_name
          -# article.rbでauthor_nameの内容を書いている
          %p= article.display_created_at
